
@{
    ViewData["Title"] = "Home Page";

    List<int> numbers = new List<int>() { 1, 2, 3 };
}

@for (int counter = 0; counter < numbers.Count; counter++)
{
    <div class="field-grp form-group @if(numbers[counter] > 2){<text>hidden</text>} col-12 col-sm-auto last">
        <h1>Some HTML</h1>
    </div>
}
